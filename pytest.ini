[pytest]
# Ensures the project root is on sys.path so `from src.xxx import yyy`
# imports work regardless of where pytest is invoked from (local machine,
# CI runner, etc.) without needing an installed package.
pythonpath = .
testpaths = tests
